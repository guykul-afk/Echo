# Echo (הד) — Canonical Design System & UI Specification

> **חוק מחייב לכל שינוי עתידי במערכת:** 
> מסמך זה מגדיר את שפת העיצוב הבלעדית והמחייבת של מערכת **Echo (הד)**.
> כל שינוי, תוספת מסך, עדכון רכיב, התאמת צבעים או טיפוגרפיה חייבים לציית ב-100% להגדרות המפורטות במסמך זה. אין להשתמש בצבעים או בפונטים שאינם מוגדרים כאן.

---

## 1. פילוסופיית העיצוב (Design Philosophy)

* **הגדרה:** *Minimalist Sacred Luxury & Epistemic Clarity* — מרחב שקט, מעמיק ויוקרתי לעיבוד שיקול דעת אנושי.
* **עקרון יסוד:** מסך נקי ורפלקטיבי המעניק תחושה של כתיבה במחברת עור יוקרתית תחת תאורה חמה, ללא עומס ויזואלי, ללא צבעי "קשת" מצועצעים (Rainbow UI), וללא רעשי ממשק מיותרים.
* **שפה וכיווניות:** עברית מלאה, כיווניות ימין-לשמאל (`dir="rtl"`).

---

## 2. פלטת הצבעים המחייבת (Strict 3-Color Hierarchy)

המערכת כולה בנויה על **3 משפחות צבע בלבד**:

### א. The Void Canvas (החלל השחור הקר)
משטח עמוק המאפשר לטקסט ולמחשבה לצוף ברוגע.
* **Base Void (רקע ראשי):** `#07080B` (שחור-בזלת עמוק)
* **Surface Card (כרטיסים ומשטחי תוכן):** `rgba(255, 255, 255, 0.03)`
* **Surface Elevated (כרטיסים מוגבהים / מודאלים):** `rgba(255, 255, 255, 0.05)`
* **Border Default (מסגרות עדינות):** `rgba(212, 175, 55, 0.15)` (זהב שקוף)
* **Border Subtle (מסגרות משניות):** `rgba(255, 255, 255, 0.08)`

### ב. Unified Typography (טיפוגרפיה מונוכרומטית מאוחדת)
כל הטקסטים במערכת חולקים גוון פנינה אחיד ובהיר למניעת עומס קריאה.
* **Text Primary (טקסט ראשי):** `#E6E8EE`
* **Text Secondary (טקסט משני / הסברים):** `rgba(230, 232, 238, 0.70)`
* **Text Tertiary / Muted (תאריכים, תוויות שקטות):** `rgba(230, 232, 238, 0.45)`
* **Text Inverse (טקסט מעל כפתור זהב מלא):** `#07080B`

### ג. Living Accent & Sacred Gold (זהב קיסרי חי)
הצבע החי והבלעדי המייצג תובנות, הארה, פעולות מרכזיות והדים מהעבר.
* **Gold Primary (זהב ראשי):** `#D4AF37`
* **Gold RGB (עבור שקיפויות):** `212, 175, 55`
* **Aura Glow (הילה וכפתורים בולטים):** `rgba(212, 175, 55, 0.25)`
* **Aura Secondary (רקע פנימי לתגיות):** `rgba(212, 175, 55, 0.12)`
* **Unified Epistemic Roles:** כל תפקידי הניתוח (מטרה, עובדה, הנחה, מסקנה, תוצאה) משתמשים אך ורק בגווני הזהב (`#D4AF37`) ואינם מפצלים צבעים לסגול/ירוק/כחול.

---

## 3. טיפוגרפיה (Typography Standards)

### פונטים מאושרים:
1. **Editorial & Headings (כותרות והכרעות):**
   * **משפחה:** `'Frank Ruhl Libre', serif` (מחלקה: `.font-editorial`)
   * **שימוש:** כותרת ראשית, כותרות דילמה, שאלת חידוד, כותרות כרטיסי עבר, מילות מפתח מודגשות.
   * **משקלים:** `600 (Semi-Bold)`, `700 (Bold)`.
2. **Body & Controls (גוף הטקסט, שדות קלט וממשק):**
   * **משפחה:** `'Assistant', sans-serif`
   * **שימוש:** גוף הטקסט, תוכן עובדות והנחות, כפתורים, תפריטים, שדות טקסט.
   * **משקלים:** `300 (Light)`, `400 (Regular)`, `600 (Semi-Bold)`.

---

## 4. רכיבי ממשק תקניים (Component Standards)

### א. כרטיס הד מהעבר (`EchoPastCard`)
* **רקע:** `rgba(212, 175, 55, 0.05)`
* **מסגרת:** `rgba(212, 175, 55, 0.25)` מעוגלת `rounded-2xl`
* **התנהגות:** מופיע מעל שאלת החידוד רק כאשר רמת הרלוונטיות היא 85%+ (`score >= 0.85`).
* **אייקון:** זהב קבוע `✨ הד מהעבר`.

### ב. שדות קלט ועריכה (`AutoResizeTextarea`)
* **עיקרון:** תחושת מחברת ללא גבולות קשיחים.
* **התנהגות:** התרחבות אוטומטית לפי תוכן ללא פסי גלילה (`resize-none overflow-hidden`).
* **רקע:** שקוף או כהה עדין `bg-white/[0.02]`.

### ג. כפתורי בחירה מרובה (Multi-Select Pills)
* **מצב רגיל:** רקע `rgba(255, 255, 255, 0.03)`, מסגרת `border-white/10`, טקסט `#E6E8EE`.
* **מצב נבחר (Active):** רקע `rgba(212, 175, 55, 0.15)`, מסגרת `#D4AF37`, טקסט `#D4AF37`.

### ד. תרשים זרימת שיקול הדעת (`DecisionFlowPipeline`)
* **מבנה:** 8 שלבי הכרעה ליניאריים מחוברים אנכית.
* **הפרדה:** קו דק עדין בצבע הזהב `border-r-2 border-amber-400/30`.
* **סטטוסים:** חתום / ביררתי (הושלם) בצבע אמרלד מאופק (`#34d399` עדין).

---

## 5. כללי איסור מוחלטים (Design Guardrails)

1. **איסור על צבעים זרחניים / מרובי גוונים:** אין להכניס כחול ניאון, אדום בוהק, או סגול.
2. **איסור על פונטים שאינם Frank Ruhl Libre או Assistant.**
3. **איסור על פינות חדות:** כל הכרטיסים והכפתורים מעוגלים (`rounded-xl` ומעלה).
4. **איסור על עומס מידע:** אם אין מידע אותנטי ברמת דיוק גבוהה (0.85+) — הממשק שומר על שקט ולא מציג כרטיסי שווא.
